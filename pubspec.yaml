name: mutanafas
description: تطبيق للصحة النفسية والعبادة وتفريغ المشاعر
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.3.0
  path: ^1.8.0
  flutter_riverpod: ^2.4.0
  intl: ^0.18.0
  fl_chart: ^0.66.0
  google_fonts: ^6.2.1
  shared_preferences: ^2.2.2
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sounds/success.mp3